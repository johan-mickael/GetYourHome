{% extends 'base.html.twig' %}

{% block title %}ProjetsEtat{% endblock %}

{% block body %}
    <h1>ProjetsEtat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projets_etat.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ projets_etat.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('projets_etat_index') }}">back to list</a>

    <a href="{{ path('projets_etat_edit', {'id': projets_etat.id}) }}">edit</a>

    {{ include('projets_etat/_delete_form.html.twig') }}
{% endblock %}
