{% extends 'base.html.twig' %}

{% block title %}ProjetsEtat index{% endblock %}

{% block body %}
    <h1>ProjetsEtat index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for projets_etat in projets_etats %}
            <tr>
                <td>{{ projets_etat.id }}</td>
                <td>{{ projets_etat.libelle }}</td>
                <td>
                    <a href="{{ path('projets_etat_show', {'id': projets_etat.id}) }}">show</a>
                    <a href="{{ path('projets_etat_edit', {'id': projets_etat.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('projets_etat_new') }}">Create new</a>
{% endblock %}
