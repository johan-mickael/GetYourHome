<form method="post" action="{{ path('projets_etat_delete', {'id': projets_etat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ projets_etat.id) }}">
    <button class="btn">Delete</button>
</form>
