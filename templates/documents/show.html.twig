{% extends 'base.html.twig' %}

{% block title %}Documents{% endblock %}

{% block body %}
<a class="btn btn-primary btn-sm" href="{{ path('documents_index') }}"><i class="fas fa-arrow-left"></i> Revenir à la liste</a>
<hr>
<h3>Détails du document</h3>
<div class="card">
	<h5 class="card-header">{{ document.name }}</h5>
	<div class="card-body">


		<a class="btn btn-secondary btn-sm" href="{{ path('documents_edit', {'id': document.id}) }}"><i class="fas fa-edit"></i> Modifier</a>

		{{ include('documents/_delete_form.html.twig') }}
	</div>
</div>


{% endblock %}