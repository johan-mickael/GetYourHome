{% extends 'base.html.twig' %}

{% block title %}Documents{% endblock %}

{% block body %}
    <h1>Documents</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ document.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('documents_index') }}">Revenir à la liste</a>

    <a class="btn btn-warning" href="{{ path('documents_edit', {'id': document.id}) }}">Modifier</a>

    {{ include('documents/_delete_form.html.twig') }}
{% endblock %}
