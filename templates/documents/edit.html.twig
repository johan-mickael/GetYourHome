{% extends 'base.html.twig' %}

{% block title %}Edit Documents{% endblock %}

{% block body %}
<a class="btn btn-primary btn-sm" href="{{ path('documents_index') }}"><i class="fas fa-arrow-left"></i> Revenir à la
	liste</a>
<hr>
<div class="float-end">
	{{ include('documents/_delete_form.html.twig') }}
</div>
<h3>Modifier le document</h3>
<div class="border p-3">
	{{ include('documents/_form.html.twig', {'button_label': 'Modifier'}) }}
</div>

{% endblock %}