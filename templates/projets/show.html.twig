{% extends 'base.html.twig' %}

{% block title %}Projets{% endblock %}

{% block body %}
    <h1>Projets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projet.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ projet.nom }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ projet.dateDebut ? projet.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ projet.dateFin ? projet.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>IdEtat</th>
                <td>{{ projet.etat.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('projets_index') }}">Revenir à la liste</a>

    <a class="btn btn-warning" href="{{ path('projets_edit', {'id': projet.id}) }}">Modifier</a>

    {{ include('projets/_delete_form.html.twig') }}
{% endblock %}
