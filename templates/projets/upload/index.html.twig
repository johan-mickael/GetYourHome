{% extends 'base.html.twig' %}

{% block title %}{{projet.nom}}{% endblock %}

{% block body %}
<h1>{{projet.nom}}</h1>
{{form_start(form)}}
{{form_widget(form)}}
<button class="btn btn-primary">{{ 'Transférer' }}</button>
{{form_end(form)}}
{% endblock %}