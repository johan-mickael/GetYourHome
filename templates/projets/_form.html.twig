{{ form_start(form) }}
{{ form_row(form.nom) }}
{{ form_row(form.dateDebut) }}
{{ form_row(form.dateFin) }}
{{ form_row(form.client) }}
{{ form_row(form.etat) }}
<div class="row">
	<div class="col-md-6">{{ form_row(form.etapes) }}</div>
	<div class="col-md-6">{{ form_row(form.documents) }}</div>
</div>
	<button class="btn btn-secondary btn-sm"><i class="fas fa-edit"></i>{{ button_label|default('Enregistrer') }}</button>

{{ form_end(form) }}

{% block javascript %}
<script>
	jQuery(function () {
		$('#projets_dateFin').attr('required', false);
	});
</script>
{% endblock %}