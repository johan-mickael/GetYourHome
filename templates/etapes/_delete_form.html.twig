<form method="post" action="{{ path('etapes_delete', {'id': etape.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer cete étape ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etape.id) }}">
    <button class="btn btn-danger btn-sm delete-btn"><i class="fas fa-trash"></i> Supprimer</button>
</form>
