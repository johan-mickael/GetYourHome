{% extends 'base.html.twig' %}

{% block title %}Etapes{% endblock %}

{% block body %}
<a class="btn btn-primary btn-sm" href="{{ path('etapes_index') }}"><i class="fas fa-arrow-left"></i> Revenir à la liste</a>
<hr>
<div class="float-end">
	{{ include('etapes/_delete_form.html.twig') }}
</div>
<h3>Détails de l'étape</h3>
<div class="card">
	<h5 class="card-header">{{ etape.libelle }}</h5>
	<div class="card-body">
		<a class="btn btn-secondary btn-sm" href="{{ path('etapes_edit', {'id': etape.id}) }}"><i class="fas fa-edit"></i> Modifier</a>

	</div>
</div>

{% endblock %}